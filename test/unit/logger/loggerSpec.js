'use strict';

describe('', function() {

  beforeEach(function() {

  });
  // Get module

  it('should load config module', function() {
    //just preparing the tests structure for a rainy sunday afternoon

    // expect(hasModule('logger.config')).to.be.ok;
  });

});
